{
    "version": "0.0.1",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch server",
            "program": "${workspaceFolder}/src/js/srv/main.js",
            "cwd": "${workspaceFolder}/"
        }
    ]
}
